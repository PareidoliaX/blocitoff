<%= form_for [@list, @item], url: list_items_path(@list, @item) do |f| %>
  <div class='form-group'>
    <%= f.label "Enter Todo Item" %>
    <%= f.text_field :name %>
  </div>
  <div class='form-group'>
    <%= f.button :submit, class: 'btn btn-success' %>
  </div>
<% end %>
